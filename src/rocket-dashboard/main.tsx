import React from "react";
import ReactDOM from "react-dom/client";
import { RocketDashboardApp } from "./RocketDashboardApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <RocketDashboardApp />
  </React.StrictMode>
);
